<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:helpers="clr-namespace:InstallationWizard.Helpers">

    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--                                                                                               -->
    <!--                              Aero 隐式样式资源字典                                             -->
    <!--                              AeroImplicitStyles.xaml                                          -->
    <!--                                                                                               -->
    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--                                                                                               -->
    <!--  本文件专门用于定义【隐式样式】(Implicit Styles)。                                              -->
    <!--                                                                                               -->
    <!--  ┌─────────────────────────────────────────────────────────────────────────────────────────┐ -->
    <!--  │                                                                                         │ -->
    <!--  │  【什么是隐式样式？】                                                                      │ -->
    <!--  │                                                                                         │ -->
    <!--  │  隐式样式是没有 x:Key 属性的样式定义，它会自动应用于作用域内所有匹配 TargetType 的控件。      │ -->
    <!--  │                                                                                         │ -->
    <!--  │  示例：                                                                                   │ -->
    <!--  │      <Style TargetType="{x:Type Button}">     ← 隐式样式（无 x:Key）                       │ -->
    <!--  │          <Setter Property="Background" Value="Blue"/>                                   │ -->
    <!--  │      </Style>                                                                           │ -->
    <!--  │                                                                                         │ -->
    <!--  │  对比显式样式：                                                                           │ -->
    <!--  │      <Style x:Key="MyButtonStyle" TargetType="{x:Type Button}">   ← 显式样式（有 x:Key） │ -->
    <!--  │          <Setter Property="Background" Value="Blue"/>                                   │ -->
    <!--  │      </Style>                                                                           │ -->
    <!--  │                                                                                         │ -->
    <!--  └─────────────────────────────────────────────────────────────────────────────────────────┘ -->
    <!--                                                                                               -->
    <!--  ┌─────────────────────────────────────────────────────────────────────────────────────────┐ -->
    <!--  │                                                                                         │ -->
    <!--  │  【作用域说明】                                                                           │ -->
    <!--  │                                                                                         │ -->
    <!--  │  本文件通过 App.xaml 的 MergedDictionaries 全局合并，因此：                                 │ -->
    <!--  │                                                                                         │ -->
    <!--  │      ● 作用域 = 整个应用程序                                                              │ -->
    <!--  │      ● 所有匹配 TargetType 的控件都会自动应用样式                                          │ -->
    <!--  │      ● 无需在控件上显式指定 Style="{StaticResource ...}"                                  │ -->
    <!--  │                                                                                         │ -->
    <!--  └─────────────────────────────────────────────────────────────────────────────────────────┘ -->
    <!--                                                                                               -->
    <!--  ┌─────────────────────────────────────────────────────────────────────────────────────────┐ -->
    <!--  │                                                                                         │ -->
    <!--  │  【推荐在此定义的隐式样式】                                                                │ -->
    <!--  │                                                                                         │ -->
    <!--  │  以下控件推荐定义隐式样式以确保全局一致的 Aero 风格：                                        │ -->
    <!--  │                                                                                         │ -->
    <!--  │      ✓ ScrollBar       - 滚动条（已实现）                                                 │ -->
    <!--  │      ○ ScrollViewer    - 滚动视图容器                                                     │ -->
    <!--  │      ✓ TextBox         - 文本输入框（已实现）                                              │ -->
    <!--  │      ✓ Button          - 通用按钮（已实现）                                                │ -->
    <!--  │      ✓ ComboBox        - 下拉框（已实现）                                                  │ -->
    <!--  │      ✓ CheckBox        - 复选框（已实现）                                                  │ -->
    <!--  │      ✓ RadioButton     - 单选按钮（已实现）                                                │ -->
    <!--  │      ✓ ListBox         - 列表框（已实现）                                                  │ -->
    <!--  │      ○ ListView        - 列表视图                                                         │ -->
    <!--  │      ○ ToolTip         - 工具提示                                                         │ -->
    <!--  │      ○ ContextMenu     - 右键菜单                                                         │ -->
    <!--  │      ○ Menu            - 菜单栏                                                           │ -->
    <!--  │      ✓ ProgressBar     - 进度条（已实现）                                                  │ -->
    <!--  │      ✓ Slider          - 滑动条（已实现）                                                  │ -->
    <!--  │                                                                                         │ -->
    <!--  │  图例：✓ 已实现  ○ 待实现                                                                 │ -->
    <!--  │                                                                                         │ -->
    <!--  └─────────────────────────────────────────────────────────────────────────────────────────┘ -->
    <!--                                                                                               -->
    <!--  ┌─────────────────────────────────────────────────────────────────────────────────────────┐ -->
    <!--  │                                                                                         │ -->
    <!--  │  【注意事项】                                                                             │ -->
    <!--  │                                                                                         │ -->
    <!--  │  1. 隐式样式会覆盖控件的默认样式，请确保完整定义所需的模板和属性                              │ -->
    <!--  │  2. 如需局部覆盖，可在控件上显式指定其他样式                                                │ -->
    <!--  │  3. 隐式样式可以引用其他资源文件中的显式样式（通过 BasedOn 或 StaticResource）               │ -->
    <!--  │  4. 本文件加载顺序应在所有依赖资源之后（如 AeroColors.xaml, AeroScrollBars.xaml）           │ -->
    <!--  │                                                                                         │ -->
    <!--  └─────────────────────────────────────────────────────────────────────────────────────────┘ -->
    <!--                                                                                               -->
    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->

    <!-- 合并依赖的资源字典 -->
    <ResourceDictionary.MergedDictionaries>
        <!-- 滚动条组件样式依赖 -->
        <ResourceDictionary Source="AeroScrollBars.xaml"/>
        <!-- 控件样式依赖（包含 ComboBox 相关画刷） -->
        <ResourceDictionary Source="AeroControls.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--                                                                                               -->
    <!--                                  ScrollBar 隐式样式                                           -->
    <!--                                                                                               -->
    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--                                                                                               -->
    <!--  此样式自动应用于所有 ScrollBar 控件，包括：                                                    -->
    <!--      ● ScrollViewer 内部自动创建的滚动条                                                       -->
    <!--      ● ListBox、ListView、DataGrid 等控件内部的滚动条                                          -->
    <!--      ● 独立使用的 ScrollBar 控件                                                               -->
    <!--                                                                                               -->
    <!--  设计说明：                                                                                    -->
    <!--      ● 轨道背景：半透明深青色 (#B2485166)，带左侧高光边框                                        -->
    <!--      ● 滑块：深灰蓝渐变，圆角 4px，带高光边框                                                    -->
    <!--      ● 两端按钮：白色三角形箭头，悬停/按下时变灰                                                 -->
    <!--      ● 支持垂直和水平两种方向（通过 Style.Triggers 切换模板）                                    -->
    <!--                                                                                               -->
    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->
    
    <Style TargetType="{x:Type ScrollBar}">
        <!-- 禁用触控笔的按住手势，防止干扰滚动操作 -->
        <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false"/>
        <!-- 禁用触控笔的轻拂手势 -->
        <Setter Property="Stylus.IsFlicksEnabled" Value="false"/>
        <!-- 背景透明，由模板内的 Border 提供实际背景 -->
        <Setter Property="Background" Value="Transparent"/>
        <!-- 前景色使用系统默认（实际未使用，因为箭头颜色在模板中硬编码为白色） -->
        <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
        <!-- 垂直滚动条宽度 -->
        <Setter Property="Width" Value="16"/>
        <Setter Property="MinWidth" Value="16"/>
        
        <!-- 垂直滚动条模板 -->
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollBar}">
                    <Grid x:Name="Bg" SnapsToDevicePixels="true">
                        <Grid.RowDefinitions>
                            <!-- 顶部按钮区域 -->
                            <RowDefinition MaxHeight="{DynamicResource {x:Static SystemParameters.VerticalScrollBarButtonHeightKey}}"/>
                            <!-- 滑块轨道区域 -->
                            <RowDefinition Height="*"/>
                            <!-- 底部按钮区域 -->
                            <RowDefinition MaxHeight="{DynamicResource {x:Static SystemParameters.VerticalScrollBarButtonHeightKey}}"/>
                        </Grid.RowDefinitions>
                        
                        <!-- 
                            轨道背景层
                            设计来源：Designs/ScrollBarTrack.xaml
                            颜色：半透明深青色，左侧有高光边框模拟玻璃边缘
                        -->
                        <Border Grid.RowSpan="3"
                                Background="#B2485166"
                                BorderThickness="1,0,0,0">
                            <Border.BorderBrush>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#0FFFFFFF" Offset="0"/>
                                    <GradientStop Color="#7FFFFFFF" Offset="0.1"/>
                                    <GradientStop Color="#00FFFFFF" Offset="0.2"/>
                                </LinearGradientBrush>
                            </Border.BorderBrush>
                        </Border>

                        <!-- 向上滚动按钮 -->
                        <RepeatButton Command="{x:Static ScrollBar.LineUpCommand}"
                                      IsEnabled="{TemplateBinding IsEnabled}"
                                      Style="{StaticResource ScrollBarPageButtonStyle}"
                                      Content="M 0 4 L 4 0 L 8 4 Z"
                                      Height="16"/>
                        
                        <!-- 滑块轨道 -->
                        <Track x:Name="PART_Track"
                               IsDirectionReversed="true"
                               IsEnabled="{TemplateBinding IsEnabled}"
                               Grid.Row="1">
                            <!-- 滑块上方的可点击区域（向上翻页） -->
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="{x:Static ScrollBar.PageUpCommand}"
                                              Style="{StaticResource ScrollBarPageButtonStyle}"
                                              Content=""
                                              Background="Transparent"
                                              Opacity="0"/>
                            </Track.DecreaseRepeatButton>
                            <!-- 滑块本体 -->
                            <Track.Thumb>
                                <Thumb Style="{StaticResource ScrollBarThumbStyle}"
                                       MinHeight="48"/>
                            </Track.Thumb>
                            <!-- 滑块下方的可点击区域（向下翻页） -->
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="{x:Static ScrollBar.PageDownCommand}"
                                              Style="{StaticResource ScrollBarPageButtonStyle}"
                                              Content=""
                                              Background="Transparent"
                                              Opacity="0"/>
                            </Track.IncreaseRepeatButton>
                        </Track>
                        
                        <!-- 向下滚动按钮 -->
                        <RepeatButton Command="{x:Static ScrollBar.LineDownCommand}"
                                      IsEnabled="{TemplateBinding IsEnabled}"
                                      Grid.Row="2"
                                      Style="{StaticResource ScrollBarPageButtonStyle}"
                                      Content="M 0 0 L 4 4 L 8 0 Z"
                                      Height="16"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        
        <!-- 水平滚动条触发器 -->
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <!-- 水平滚动条尺寸属性 -->
                <Setter Property="Width" Value="Auto"/>
                <Setter Property="MinWidth" Value="0"/>
                <Setter Property="Height" Value="16"/>
                <Setter Property="MinHeight" Value="16"/>
                
                <!-- 水平滚动条模板 -->
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ScrollBar}">
                            <Grid x:Name="Bg" SnapsToDevicePixels="true">
                                <Grid.ColumnDefinitions>
                                    <!-- 左侧按钮区域 -->
                                    <ColumnDefinition MaxWidth="{DynamicResource {x:Static SystemParameters.HorizontalScrollBarButtonWidthKey}}"/>
                                    <!-- 滑块轨道区域 -->
                                    <ColumnDefinition Width="*"/>
                                    <!-- 右侧按钮区域 -->
                                    <ColumnDefinition MaxWidth="{DynamicResource {x:Static SystemParameters.HorizontalScrollBarButtonWidthKey}}"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- 
                                    水平轨道背景层
                                    与垂直版本相同的设计，但边框在顶部
                                -->
                                <Border Grid.ColumnSpan="3"
                                        Background="#B2485166"
                                        BorderThickness="0,1,0,0">
                                    <Border.BorderBrush>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#0FFFFFFF" Offset="0"/>
                                            <GradientStop Color="#7FFFFFFF" Offset="0.1"/>
                                            <GradientStop Color="#00FFFFFF" Offset="0.2"/>
                                        </LinearGradientBrush>
                                    </Border.BorderBrush>
                                </Border>

                                <!-- 向左滚动按钮 -->
                                <RepeatButton Command="{x:Static ScrollBar.LineLeftCommand}"
                                              IsEnabled="{TemplateBinding IsEnabled}"
                                              Style="{StaticResource ScrollBarPageButtonStyle}"
                                              Content="M 4 0 L 0 4 L 4 8 Z"
                                              Width="16"/>
                                
                                <!-- 滑块轨道 -->
                                <Track x:Name="PART_Track"
                                       Grid.Column="1"
                                       IsEnabled="{TemplateBinding IsEnabled}">
                                    <!-- 滑块左侧的可点击区域（向左翻页） -->
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton Command="{x:Static ScrollBar.PageLeftCommand}"
                                                      Style="{StaticResource ScrollBarPageButtonStyle}"
                                                      Content=""
                                                      Background="Transparent"
                                                      Opacity="0"/>
                                    </Track.DecreaseRepeatButton>
                                    <!-- 滑块本体（使用水平专用样式） -->
                                    <Track.Thumb>
                                        <Thumb Style="{StaticResource ScrollBarThumbStyleHorizontal}"
                                               MinWidth="48"/>
                                    </Track.Thumb>
                                    <!-- 滑块右侧的可点击区域（向右翻页） -->
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton Command="{x:Static ScrollBar.PageRightCommand}"
                                                      Style="{StaticResource ScrollBarPageButtonStyle}"
                                                      Content=""
                                                      Background="Transparent"
                                                      Opacity="0"/>
                                    </Track.IncreaseRepeatButton>
                                </Track>
                                
                                <!-- 向右滚动按钮 -->
                                <RepeatButton Command="{x:Static ScrollBar.LineRightCommand}"
                                              IsEnabled="{TemplateBinding IsEnabled}"
                                              Grid.Column="2"
                                              Style="{StaticResource ScrollBarPageButtonStyle}"
                                              Content="M 0 0 L 4 4 L 0 8 Z"
                                              Width="16"/>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--                                                                                               -->
    <!--                              待实现的隐式样式占位区域                                          -->
    <!--                                                                                               -->
    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->
    
    <!-- 
        TODO: 以下隐式样式待实现
        
        <Style TargetType="{x:Type ToolTip}">
            需要实现：Aero 风格的工具提示，半透明玻璃背景
        </Style>
    -->

    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--                                                                                               -->
    <!--                                  TextBox 隐式样式                                             -->
    <!--                                                                                               -->
    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--                                                                                               -->
    <!--  此样式自动应用于所有 TextBox 控件，提供 Aero 风格的输入框外观。                                 -->
    <!--  设计来源：Designs/TextboxIdle.xaml, TextboxHover.xaml, TextboxActivated.xaml                  -->
    <!--                                                                                               -->
    <!--  设计说明：                                                                                    -->
    <!--      ● 闲置状态：圆角边框，底部青蓝色径向发光，白色渐变边框                                       -->
    <!--      ● 悬停状态：顶部增加白色高光层，边框更亮                                                    -->
    <!--      ● 激活状态：顶部和底部双重青蓝色发光，表示输入焦点                                          -->
    <!--      ● 边框：径向渐变从青蓝色(#5984AD)到白色                                                    -->
    <!--                                                                                               -->
    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->

    <Style TargetType="{x:Type TextBox}">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="CaretBrush" Value="White"/>
        <Setter Property="SelectionBrush" Value="#804B9DCC"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="MinHeight" Value="28"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <!-- 启用"首次点击全选"行为：点击一次全选，点击两次定位光标 -->
        <Setter Property="helpers:TextBoxBehaviors.SelectAllOnFirstClick" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <Grid>
                        <!-- ═══════════════════════════════════════════════════════════════════════ -->
                        <!-- 闲置状态 - 基于 TextboxIdle.xaml                                          -->
                        <!-- ═══════════════════════════════════════════════════════════════════════ -->
                        
                        <!-- 闲置状态：主背景层 - 底部青蓝色径向发光填充 -->
                        <Border x:Name="IdleBackground" CornerRadius="4" BorderThickness="1">
                            <Border.BorderBrush>
                                <RadialGradientBrush RadiusX="1.176" RadiusY="0.666" Center="0.5,1.106" GradientOrigin="0.5,1.106">
                                    <GradientStop Color="#FF5984AD" Offset="0"/>
                                    <GradientStop Color="#FFFFFFFF" Offset="1"/>
                                </RadialGradientBrush>
                            </Border.BorderBrush>
                            <Border.Background>
                                <RadialGradientBrush RadiusX="1.182" RadiusY="0.679" Center="0.5,1.119" GradientOrigin="0.5,1.119">
                                    <GradientStop Color="#FF4588BD" Offset="0"/>
                                    <GradientStop Color="#001AD5FF" Offset="0.381"/>
                                </RadialGradientBrush>
                            </Border.Background>
                        </Border>
                        
                        <!-- ═══════════════════════════════════════════════════════════════════════ -->
                        <!-- 悬停状态 - 基于 TextboxHover.xaml                                         -->
                        <!-- ═══════════════════════════════════════════════════════════════════════ -->
                        
                        <!-- 悬停状态：顶部白色高光层 -->
                        <Border x:Name="HoverHighlight" Opacity="0" CornerRadius="4" BorderThickness="1" IsHitTestVisible="False">
                            <Border.BorderBrush>
                                <RadialGradientBrush RadiusX="3.59" RadiusY="1.925" Center="0.5,-0.072" GradientOrigin="0.5,-0.072">
                                    <GradientStop Color="#FFFFFFFF" Offset="0"/>
                                    <GradientStop Color="#34C3EFFF" Offset="1"/>
                                </RadialGradientBrush>
                            </Border.BorderBrush>
                            <Border.Background>
                                <RadialGradientBrush RadiusX="3.59" RadiusY="1.925" Center="0.5,-0.072" GradientOrigin="0.5,-0.072">
                                    <GradientStop Color="#44FFFFFF" Offset="0"/>
                                    <GradientStop Color="#0BFFFFFF" Offset="0.345"/>
                                    <GradientStop Color="#01FFFFFF" Offset="0.351"/>
                                    <GradientStop Color="#00FFFFFF" Offset="1"/>
                                </RadialGradientBrush>
                            </Border.Background>
                        </Border>
                        
                        <!-- 悬停状态：主背景层 - 顶部青蓝色径向发光 -->
                        <Border x:Name="HoverBackground" Opacity="0" CornerRadius="4" BorderThickness="1" IsHitTestVisible="False">
                            <Border.BorderBrush>
                                <RadialGradientBrush RadiusX="1.176" RadiusY="0.666" Center="0.5,-0.187" GradientOrigin="0.5,-0.187">
                                    <GradientStop Color="#FF5984AD" Offset="0"/>
                                    <GradientStop Color="#FFFFFFFF" Offset="1"/>
                                </RadialGradientBrush>
                            </Border.BorderBrush>
                            <Border.Background>
                                <RadialGradientBrush RadiusX="1.182" RadiusY="0.679" Center="0.5,-0.201" GradientOrigin="0.5,-0.201">
                                    <GradientStop Color="#384588BD" Offset="0"/>
                                    <GradientStop Color="#001AD5FF" Offset="0.691"/>
                                </RadialGradientBrush>
                            </Border.Background>
                        </Border>
                        
                        <!-- ═══════════════════════════════════════════════════════════════════════ -->
                        <!-- 激活/聚焦状态 - 基于 TextboxActivated.xaml                                 -->
                        <!-- ═══════════════════════════════════════════════════════════════════════ -->
                        
                        <!-- 激活状态：顶部青蓝色径向发光（增强范围和强度） -->
                        <Border x:Name="FocusedTopGlow" Opacity="0" CornerRadius="4" BorderThickness="1" IsHitTestVisible="False">
                            <Border.BorderBrush>
                                <RadialGradientBrush RadiusX="1.5" RadiusY="0.9" Center="0.5,-0.2" GradientOrigin="0.5,-0.2">
                                    <GradientStop Color="#FF6A9FC0" Offset="0"/>
                                    <GradientStop Color="#FFFFFFFF" Offset="1"/>
                                </RadialGradientBrush>
                            </Border.BorderBrush>
                            <Border.Background>
                                <RadialGradientBrush RadiusX="1.5" RadiusY="0.9" Center="0.5,-0.2" GradientOrigin="0.5,-0.2">
                                    <GradientStop Color="#FF5A9ED0" Offset="0"/>
                                    <GradientStop Color="#001AD5FF" Offset="0.55"/>
                                </RadialGradientBrush>
                            </Border.Background>
                        </Border>
                        
                        <!-- 激活状态：底部青蓝色径向发光（增强范围和强度） -->
                        <Border x:Name="FocusedBottomGlow" Opacity="0" CornerRadius="4" BorderThickness="1" IsHitTestVisible="False">
                            <Border.BorderBrush>
                                <RadialGradientBrush RadiusX="1.5" RadiusY="0.9" Center="0.5,1.2" GradientOrigin="0.5,1.2">
                                    <GradientStop Color="#FF6A9FC0" Offset="0"/>
                                    <GradientStop Color="#FFFFFFFF" Offset="1"/>
                                </RadialGradientBrush>
                            </Border.BorderBrush>
                            <Border.Background>
                                <RadialGradientBrush RadiusX="1.5" RadiusY="0.9" Center="0.5,1.2" GradientOrigin="0.5,1.2">
                                    <GradientStop Color="#FF5A9ED0" Offset="0"/>
                                    <GradientStop Color="#001AD5FF" Offset="0.55"/>
                                </RadialGradientBrush>
                            </Border.Background>
                        </Border>
                        
                        <!-- ═══════════════════════════════════════════════════════════════════════ -->
                        <!-- 内阴影效果 - 使用传统渐变实现                                              -->
                        <!-- ═══════════════════════════════════════════════════════════════════════ -->
                        
                        <!-- 顶部内阴影 -->
                        <Border CornerRadius="4,4,0,0" Height="8" VerticalAlignment="Top" IsHitTestVisible="False">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Color="#40000000" Offset="0"/>
                                    <GradientStop Color="#00000000" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                        
                        <!-- 左侧内阴影 -->
                        <Border CornerRadius="4,0,0,4" Width="6" HorizontalAlignment="Left" IsHitTestVisible="False">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#25000000" Offset="0"/>
                                    <GradientStop Color="#00000000" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                        
                        <!-- 右侧内阴影 -->
                        <Border CornerRadius="0,4,4,0" Width="6" HorizontalAlignment="Right" IsHitTestVisible="False">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="1,0" EndPoint="0,0">
                                    <GradientStop Color="#25000000" Offset="0"/>
                                    <GradientStop Color="#00000000" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                        
                        <!-- 文本内容层 - 独立矩形，不受其他层级影响 -->
                        <Border Margin="1" CornerRadius="4" Background="Transparent">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="0,0"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          Focusable="False"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden">
                                <ScrollViewer.Effect>
                                    <DropShadowEffect Color="#000000" BlurRadius="2" ShadowDepth="1" Opacity="0.3"/>
                                </ScrollViewer.Effect>
                            </ScrollViewer>
                        </Border>
                    </Grid>
                    
                    <ControlTemplate.Triggers>
                        <!-- 悬停状态触发器 -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard x:Name="HoverEnterStoryboard">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="HoverHighlight" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.15"/>
                                        <DoubleAnimation Storyboard.TargetName="HoverBackground" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <StopStoryboard BeginStoryboardName="HoverEnterStoryboard"/>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="HoverHighlight" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15"/>
                                        <DoubleAnimation Storyboard.TargetName="HoverBackground" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        
                        <!-- 聚焦状态触发器 -->
                        <Trigger Property="IsFocused" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard x:Name="FocusEnterStoryboard">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="IdleBackground" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15"/>
                                        <DoubleAnimation Storyboard.TargetName="HoverHighlight" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15"/>
                                        <DoubleAnimation Storyboard.TargetName="HoverBackground" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15"/>
                                        <DoubleAnimation Storyboard.TargetName="FocusedTopGlow" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.15"/>
                                        <DoubleAnimation Storyboard.TargetName="FocusedBottomGlow" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <StopStoryboard BeginStoryboardName="FocusEnterStoryboard"/>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="IdleBackground" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.15"/>
                                        <DoubleAnimation Storyboard.TargetName="FocusedTopGlow" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15"/>
                                        <DoubleAnimation Storyboard.TargetName="FocusedBottomGlow" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        
                        <!-- 键盘聚焦状态触发器 -->
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="IdleBackground" Property="Opacity" Value="0"/>
                            <Setter TargetName="FocusedTopGlow" Property="Opacity" Value="1"/>
                            <Setter TargetName="FocusedBottomGlow" Property="Opacity" Value="1"/>
                        </Trigger>
                        
                        <!-- 禁用状态触发器 -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                        
                        <!-- 只读状态触发器 -->
                        <Trigger Property="IsReadOnly" Value="True">
                            <Setter Property="Opacity" Value="0.7"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--                                                                                               -->
    <!--                                  Button 隐式样式                                              -->
    <!--                                                                                               -->
    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--                                                                                               -->
    <!--  此样式自动应用于所有 Button 控件，提供 Aero 风格的按钮外观。                                    -->
    <!--  设计来源：Designs/ImplicitButtonIdle.xaml, ImplicitButtonHover.xaml, ImplicitButtonPressed.xaml -->
    <!--                                                                                               -->
    <!--  设计说明：                                                                                    -->
    <!--      ● 闲置状态：深蓝渐变背景，底部青色径向发光                                                  -->
    <!--      ● 悬停状态：更亮的蓝色渐变，顶部和底部双重发光                                              -->
    <!--      ● 按下状态：更深的蓝色渐变，带内阴影效果                                                    -->
    <!--      ● 边框：半透明青蓝色 (#67BBDDF2)                                                          -->
    <!--                                                                                               -->
    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->

    <Style TargetType="{x:Type Button}">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="16,8"/>
        <Setter Property="MinHeight" Value="28"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Grid>
                        <!-- ═══════════════════════════════════════════════════════════════════════ -->
                        <!-- 闲置状态 - 基于 ImplicitButtonIdle.xaml                                   -->
                        <!-- ═══════════════════════════════════════════════════════════════════════ -->
                        
                        <!-- 闲置状态：主背景层 - 深蓝渐变 -->
                        <Border x:Name="IdleBackground" CornerRadius="4" BorderThickness="2" BorderBrush="#67BBDDF2">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0.5,-0.887" EndPoint="0.5,1.887">
                                    <GradientStop Color="#FF637495" Offset="0.308"/>
                                    <GradientStop Color="#FF384D75" Offset="0.489"/>
                                    <GradientStop Color="#FF223761" Offset="0.495"/>
                                    <GradientStop Color="#FF284D7E" Offset="0.681"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                        
                        <!-- 闲置状态：底部青色径向发光 -->
                        <Border x:Name="IdleGlow" CornerRadius="4" IsHitTestVisible="False">
                            <Border.Background>
                                <RadialGradientBrush RadiusX="0.67" RadiusY="1.43" Center="0.5,1.32" GradientOrigin="0.5,1.32">
                                    <GradientStop Color="#FF4B9DCC" Offset="0.231"/>
                                    <GradientStop Color="#013C4F73" Offset="1"/>
                                </RadialGradientBrush>
                            </Border.Background>
                        </Border>
                        
                        <!-- ═══════════════════════════════════════════════════════════════════════ -->
                        <!-- 悬停状态 - 基于 ImplicitButtonHover.xaml                                  -->
                        <!-- ═══════════════════════════════════════════════════════════════════════ -->
                        
                        <!-- 悬停状态：主背景层 - 更亮的蓝色渐变 -->
                        <Border x:Name="HoverBackground" Opacity="0" CornerRadius="4" BorderThickness="2" BorderBrush="#67BBDDF2">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0.5,-0.887" EndPoint="0.5,1.887">
                                    <GradientStop Color="#FF7387AF" Offset="0.308"/>
                                    <GradientStop Color="#FF405886" Offset="0.489"/>
                                    <GradientStop Color="#FF284276" Offset="0.495"/>
                                    <GradientStop Color="#FF295691" Offset="0.681"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                        
                        <!-- 悬停状态：底部青色径向发光 -->
                        <Border x:Name="HoverBottomGlow" Opacity="0" CornerRadius="4" IsHitTestVisible="False">
                            <Border.Background>
                                <RadialGradientBrush RadiusX="0.56" RadiusY="1.0" Center="0.5,1.23" GradientOrigin="0.5,1.23">
                                    <GradientStop Color="#FF4B9DCC" Offset="0.231"/>
                                    <GradientStop Color="#013C4F73" Offset="1"/>
                                </RadialGradientBrush>
                            </Border.Background>
                        </Border>
                        
                        <!-- 悬停状态：顶部青色径向发光 -->
                        <Border x:Name="HoverTopGlow" Opacity="0" CornerRadius="4" IsHitTestVisible="False">
                            <Border.Background>
                                <RadialGradientBrush RadiusX="0.93" RadiusY="0.79" Center="0.5,-0.2" GradientOrigin="0.5,-0.2">
                                    <GradientStop Color="#FF4B9DCC" Offset="0.231"/>
                                    <GradientStop Color="#013C4F73" Offset="1"/>
                                </RadialGradientBrush>
                            </Border.Background>
                        </Border>
                        
                        <!-- ═══════════════════════════════════════════════════════════════════════ -->
                        <!-- 按下状态 - 基于 ImplicitButtonPressed.xaml + 内阴影                       -->
                        <!-- ═══════════════════════════════════════════════════════════════════════ -->
                        
                        <!-- 按下状态：主背景层 - 更深的蓝色渐变 -->
                        <Border x:Name="PressedBackground" Opacity="0" CornerRadius="4" BorderThickness="2" BorderBrush="#67BBDDF2">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0.5,-0.887" EndPoint="0.5,1.887">
                                    <GradientStop Color="#FF324F80" Offset="0.308"/>
                                    <GradientStop Color="#FF142E74" Offset="0.489"/>
                                    <GradientStop Color="#FF09246B" Offset="0.501"/>
                                    <GradientStop Color="#FF0A348A" Offset="0.681"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                        
                        <!-- 按下状态：底部蓝紫色径向发光 -->
                        <Border x:Name="PressedGlow" Opacity="0" CornerRadius="4" IsHitTestVisible="False">
                            <Border.Background>
                                <RadialGradientBrush RadiusX="0.69" RadiusY="0.55" Center="0.5,1.12" GradientOrigin="0.5,1.12">
                                    <GradientStop Color="#FF3A5AC6" Offset="0.213"/>
                                    <GradientStop Color="#013C4F73" Offset="1"/>
                                </RadialGradientBrush>
                            </Border.Background>
                        </Border>
                        
                        <!-- 按下状态：内阴影效果 - 模拟按下凹陷感 -->
                        <Border x:Name="PressedInnerShadow" Opacity="0" CornerRadius="4" IsHitTestVisible="False">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Color="#80000000" Offset="0"/>
                                    <GradientStop Color="#40000000" Offset="0.15"/>
                                    <GradientStop Color="#00000000" Offset="0.4"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                        
                        <!-- 按下状态：左右内阴影 -->
                        <Border x:Name="PressedSideShadow" Opacity="0" CornerRadius="4" IsHitTestVisible="False">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                    <GradientStop Color="#50000000" Offset="0"/>
                                    <GradientStop Color="#00000000" Offset="0.1"/>
                                    <GradientStop Color="#00000000" Offset="0.9"/>
                                    <GradientStop Color="#50000000" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                        
                        <!-- 内容呈现器 -->
                        <ContentPresenter x:Name="ContentPresenter"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          RecognizesAccessKey="True">
                            <ContentPresenter.Effect>
                                <DropShadowEffect Color="#000000" BlurRadius="2" ShadowDepth="1" Opacity="0.5"/>
                            </ContentPresenter.Effect>
                        </ContentPresenter>
                    </Grid>
                    
                    <ControlTemplate.Triggers>
                        <!-- 悬停状态触发器 -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard x:Name="HoverEnterStoryboard">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="IdleBackground" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15"/>
                                        <DoubleAnimation Storyboard.TargetName="IdleGlow" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15"/>
                                        <DoubleAnimation Storyboard.TargetName="HoverBackground" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.15"/>
                                        <DoubleAnimation Storyboard.TargetName="HoverBottomGlow" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.15"/>
                                        <DoubleAnimation Storyboard.TargetName="HoverTopGlow" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <!-- 停止进入动画，防止动画冲突导致状态卡住 -->
                                <StopStoryboard BeginStoryboardName="HoverEnterStoryboard"/>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="IdleBackground" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.15"/>
                                        <DoubleAnimation Storyboard.TargetName="IdleGlow" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.15"/>
                                        <DoubleAnimation Storyboard.TargetName="HoverBackground" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15"/>
                                        <DoubleAnimation Storyboard.TargetName="HoverBottomGlow" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15"/>
                                        <DoubleAnimation Storyboard.TargetName="HoverTopGlow" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        
                        <!-- 按下状态触发器 -->
                        <Trigger Property="IsPressed" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard x:Name="PressedEnterStoryboard">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="IdleBackground" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.08"/>
                                        <DoubleAnimation Storyboard.TargetName="IdleGlow" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.08"/>
                                        <DoubleAnimation Storyboard.TargetName="HoverBackground" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.08"/>
                                        <DoubleAnimation Storyboard.TargetName="HoverBottomGlow" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.08"/>
                                        <DoubleAnimation Storyboard.TargetName="HoverTopGlow" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.08"/>
                                        <DoubleAnimation Storyboard.TargetName="PressedBackground" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.08"/>
                                        <DoubleAnimation Storyboard.TargetName="PressedGlow" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.08"/>
                                        <DoubleAnimation Storyboard.TargetName="PressedInnerShadow" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.08"/>
                                        <DoubleAnimation Storyboard.TargetName="PressedSideShadow" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.08"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <!-- 先停止进入动画，防止动画冲突 -->
                                <StopStoryboard BeginStoryboardName="PressedEnterStoryboard"/>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <!-- 清除 Pressed 状态 -->
                                        <DoubleAnimation Storyboard.TargetName="PressedBackground" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15"/>
                                        <DoubleAnimation Storyboard.TargetName="PressedGlow" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15"/>
                                        <DoubleAnimation Storyboard.TargetName="PressedInnerShadow" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15"/>
                                        <DoubleAnimation Storyboard.TargetName="PressedSideShadow" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        
                        <!-- 使用 MultiTrigger 处理：按下释放后仍在悬停状态 -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsPressed" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="HoverBackground" Property="Opacity" Value="1"/>
                            <Setter TargetName="HoverBottomGlow" Property="Opacity" Value="1"/>
                            <Setter TargetName="HoverTopGlow" Property="Opacity" Value="1"/>
                            <Setter TargetName="IdleBackground" Property="Opacity" Value="0"/>
                            <Setter TargetName="IdleGlow" Property="Opacity" Value="0"/>
                        </MultiTrigger>
                        
                        <!-- 禁用状态触发器 -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--                                                                                               -->
    <!--                                  ComboBox 隐式样式                                            -->
    <!--                                                                                               -->
    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--                                                                                               -->
    <!--  此样式自动应用于所有 ComboBox 控件，提供 Aero 风格的下拉框外观。                                -->
    <!--  设计来源：Designs/DropDownIdle.xaml, DropDownHover.xaml, DropDownPressed.xaml                 -->
    <!--                                                                                               -->
    <!--  设计说明：                                                                                    -->
    <!--      ● 闲置状态：深蓝渐变背景，带边框                                                           -->
    <!--      ● 悬停状态：蓝色高光效果                                                                  -->
    <!--      ● 按下状态：更深的蓝色渐变                                                                 -->
    <!--      ● 下拉框：Aero 玻璃质感背景，深色渐变 + 高光边框                                            -->
    <!--                                                                                               -->
    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->

    <!-- ComboBox 下拉框项悬停背景 -->
    <LinearGradientBrush x:Key="ComboBoxItemHoverBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#60A0D0FF" Offset="0"/>
        <GradientStop Color="#3060A0D0" Offset="0.5"/>
        <GradientStop Color="#4080C0F0" Offset="1"/>
    </LinearGradientBrush>

    <!-- ComboBox 下拉框项选中背景 -->
    <LinearGradientBrush x:Key="ComboBoxItemSelectedBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#A0C0E8FF" Offset="0"/>
        <GradientStop Color="#6080B0E0" Offset="0.5"/>
        <GradientStop Color="#80A0D0FF" Offset="1"/>
    </LinearGradientBrush>

    <!-- ComboBoxItem 隐式样式 -->
    <Style TargetType="{x:Type ComboBoxItem}">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                    <Grid>
                        <!-- 悬停/选中背景 -->
                        <Border x:Name="Bg" 
                                Background="Transparent" 
                                CornerRadius="2"
                                BorderThickness="1"
                                BorderBrush="Transparent"/>
                        <!-- 顶部高光 -->
                        <Border x:Name="TopHighlight"
                                CornerRadius="2,2,0,0"
                                Height="12"
                                VerticalAlignment="Top"
                                Opacity="0">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Color="#40FFFFFF" Offset="0"/>
                                    <GradientStop Color="#00FFFFFF" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                        <ContentPresenter Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="{StaticResource ComboBoxItemHoverBrush}"/>
                            <Setter TargetName="Bg" Property="BorderBrush" Value="#5090C0E0"/>
                            <Setter TargetName="TopHighlight" Property="Opacity" Value="0.6"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="{StaticResource ComboBoxItemSelectedBrush}"/>
                            <Setter TargetName="Bg" Property="BorderBrush" Value="#80A0D0FF"/>
                            <Setter TargetName="TopHighlight" Property="Opacity" Value="0.8"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ComboBox}">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="5,2"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBox}">
                    <Grid>
                        <ToggleButton Name="ToggleButton"
                                      Focusable="false"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Grid Background="Transparent">
                                        <!-- 基础背景层 - 始终可见 -->
                                        <Border x:Name="IdleBackground" CornerRadius="3" BorderThickness="1" BorderBrush="#FF82869E">
                                            <Border.Background>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                    <GradientStop Color="#E0183858" Offset="0"/>
                                                    <GradientStop Color="#D0285878" Offset="0.15"/>
                                                    <GradientStop Color="#C0306888" Offset="0.5"/>
                                                    <GradientStop Color="#D0285878" Offset="0.85"/>
                                                    <GradientStop Color="#E0183858" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Border.Background>
                                        </Border>
                                        
                                        <!-- 闲置状态顶部玻璃高光 -->
                                        <Border CornerRadius="3,3,0,0" Height="12" VerticalAlignment="Top" IsHitTestVisible="False">
                                            <Border.Background>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                    <GradientStop Color="#30FFFFFF" Offset="0"/>
                                                    <GradientStop Color="#10FFFFFF" Offset="0.5"/>
                                                    <GradientStop Color="#00FFFFFF" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Border.Background>
                                        </Border>
                                        
                                        <!-- 闲置状态底部发光 -->
                                        <Border CornerRadius="0,0,3,3" Height="10" VerticalAlignment="Bottom" IsHitTestVisible="False">
                                            <Border.Background>
                                                <RadialGradientBrush RadiusX="0.8" RadiusY="2" Center="0.5,1" GradientOrigin="0.5,1">
                                                    <GradientStop Color="#4060B0F0" Offset="0"/>
                                                    <GradientStop Color="#0060B0F0" Offset="1"/>
                                                </RadialGradientBrush>
                                            </Border.Background>
                                        </Border>
                                        
                                        <!-- 闲置状态边框高光 -->
                                        <Border CornerRadius="3" BorderThickness="1" IsHitTestVisible="False">
                                            <Border.BorderBrush>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                    <GradientStop Color="#50FFFFFF" Offset="0"/>
                                                    <GradientStop Color="#20FFFFFF" Offset="0.5"/>
                                                    <GradientStop Color="#3080B0D0" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Border.BorderBrush>
                                        </Border>
                                        
                                        <!-- 悬停状态背景 -->
                                        <Border x:Name="HoverBackground" Opacity="0" CornerRadius="3" BorderThickness="1" BorderBrush="#67BBDDF2">
                                            <Border.Background>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                    <GradientStop Color="#CD6E869C" Offset="0"/>
                                                    <GradientStop Color="#CD3A576E" Offset="0.35"/>
                                                    <GradientStop Color="#CD162D41" Offset="0.5"/>
                                                    <GradientStop Color="#CB4C87AF" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Border.Background>
                                        </Border>
                                        
                                        <!-- 悬停状态顶部高光 -->
                                        <Border x:Name="HoverTopHighlight" Opacity="0" CornerRadius="3,3,0,0" Height="14" VerticalAlignment="Top" IsHitTestVisible="False">
                                            <Border.Background>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                    <GradientStop Color="#50FFFFFF" Offset="0"/>
                                                    <GradientStop Color="#20FFFFFF" Offset="0.5"/>
                                                    <GradientStop Color="#00FFFFFF" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Border.Background>
                                        </Border>
                                        
                                        <!-- 按下状态背景 -->
                                        <Border x:Name="PressedBackground" Opacity="0" CornerRadius="3" BorderThickness="1" BorderBrush="#67BBDDF2">
                                            <Border.Background>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                    <GradientStop Color="#FF87B0CA" Offset="0"/>
                                                    <GradientStop Color="#FF496A89" Offset="0.45"/>
                                                    <GradientStop Color="#FF335876" Offset="0.5"/>
                                                    <GradientStop Color="#FF559EBA" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Border.Background>
                                        </Border>
                                        
                                        <!-- 按下状态顶部高光 -->
                                        <Border x:Name="PressedTopHighlight" Opacity="0" CornerRadius="3,3,0,0" Height="10" VerticalAlignment="Top" IsHitTestVisible="False">
                                            <Border.Background>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                    <GradientStop Color="#60FFFFFF" Offset="0"/>
                                                    <GradientStop Color="#20FFFFFF" Offset="0.6"/>
                                                    <GradientStop Color="#00FFFFFF" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Border.Background>
                                        </Border>
                                        
                                        <!-- 下拉箭头 -->
                                        <Path Data="M 0 0 L 4 4 L 8 0 Z" Fill="White" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0" IsHitTestVisible="False">
                                            <Path.Effect>
                                                <DropShadowEffect Color="#000000" BlurRadius="2" ShadowDepth="1" Opacity="0.5"/>
                                            </Path.Effect>
                                        </Path>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Trigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="HoverBackground" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.15"/>
                                                        <DoubleAnimation Storyboard.TargetName="HoverTopHighlight" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.15"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.EnterActions>
                                            <Trigger.ExitActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="HoverBackground" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15"/>
                                                        <DoubleAnimation Storyboard.TargetName="HoverTopHighlight" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.ExitActions>
                                        </Trigger>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Trigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="PressedBackground" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.1"/>
                                                        <DoubleAnimation Storyboard.TargetName="PressedTopHighlight" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.1"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.EnterActions>
                                            <Trigger.ExitActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="PressedBackground" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15"/>
                                                        <DoubleAnimation Storyboard.TargetName="PressedTopHighlight" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.ExitActions>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>

                        <ContentPresenter Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          IsHitTestVisible="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"/>
                        
                        <!-- 
                            ═══════════════════════════════════════════════════════════════════════════
                            [已禁用] 基于 Windows API 的模糊效果尝试
                            原因：SetWindowCompositionAttribute 在 Popup 上效果不稳定
                            保留代码以供将来参考：helpers:PopupBlurHelper.IsBlurEnabled="True"
                            ═══════════════════════════════════════════════════════════════════════════
                        -->
                        <Popup x:Name="PART_Popup" 
                               AllowsTransparency="True" 
                               IsOpen="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" 
                               Placement="Bottom" 
                               PopupAnimation="Slide">
                            <Grid MaxHeight="{TemplateBinding MaxDropDownHeight}" MinWidth="{TemplateBinding ActualWidth}" Margin="2">
                                <!-- 阴影层 -->
                                <Border CornerRadius="4" Margin="-2">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#000000" BlurRadius="12" ShadowDepth="2" Opacity="0.6"/>
                                    </Border.Effect>
                                    <Border.Background>
                                        <SolidColorBrush Color="#01000000"/>
                                    </Border.Background>
                                </Border>
                                
                                <!-- 主背景层 - Aero 深色玻璃渐变 -->
                                <Border x:Name="DropDownBorder" CornerRadius="4" Opacity="0">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#F0102030" Offset="0"/>
                                            <GradientStop Color="#F0183050" Offset="0.3"/>
                                            <GradientStop Color="#F0102840" Offset="0.7"/>
                                            <GradientStop Color="#F0081828" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <Border.Triggers>
                                        <EventTrigger RoutedEvent="Border.Loaded">
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.2"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </EventTrigger>
                                    </Border.Triggers>
                                </Border>
                                
                                <!-- 顶部高光层 - 模拟玻璃反光 -->
                                <Border CornerRadius="4,4,0,0" Height="20" VerticalAlignment="Top" IsHitTestVisible="False">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#25FFFFFF" Offset="0"/>
                                            <GradientStop Color="#10FFFFFF" Offset="0.5"/>
                                            <GradientStop Color="#00FFFFFF" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                </Border>
                                
                                <!-- 底部发光层 - Aero 特征性的底部青色光晕 -->
                                <Border CornerRadius="0,0,4,4" Height="30" VerticalAlignment="Bottom" IsHitTestVisible="False">
                                    <Border.Background>
                                        <RadialGradientBrush RadiusX="0.8" RadiusY="1.5" Center="0.5,1" GradientOrigin="0.5,1">
                                            <GradientStop Color="#3040A0E0" Offset="0"/>
                                            <GradientStop Color="#0040A0E0" Offset="1"/>
                                        </RadialGradientBrush>
                                    </Border.Background>
                                </Border>
                                
                                <!-- 边框层 - Aero 玻璃边框效果 -->
                                <Border CornerRadius="4" BorderThickness="1" IsHitTestVisible="False">
                                    <Border.BorderBrush>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#60FFFFFF" Offset="0"/>
                                            <GradientStop Color="#30FFFFFF" Offset="0.3"/>
                                            <GradientStop Color="#20FFFFFF" Offset="0.7"/>
                                            <GradientStop Color="#4080C0E0" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.BorderBrush>
                                </Border>
                                
                                <!-- 内容区域 -->
                                <ScrollViewer Margin="4,6">
                                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                </ScrollViewer>
                            </Grid>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--                                                                                               -->
    <!--                                  Slider 隐式样式                                              -->
    <!--                                                                                               -->
    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--                                                                                               -->
    <!--  此样式自动应用于所有 Slider 控件，提供 Aero 风格的滑动条外观。                                  -->
    <!--  设计来源：PlayerStyles.xaml 中的 PlayerSliderStyle                                            -->
    <!--                                                                                               -->
    <!--  设计说明：                                                                                    -->
    <!--      ● 轨道背景：半透明深色，带内阴影效果                                                       -->
    <!--      ● 进度填充：蓝色渐变，Aero 风格的发光效果                                                  -->
    <!--      ● 滑块：白色圆角矩形，带阴影和悬停效果                                                     -->
    <!--      ● 支持水平和垂直两种方向                                                                  -->
    <!--                                                                                               -->
    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->

    <!-- Slider 轨道 RepeatButton 样式 (透明，用于点击定位) -->
    <Style x:Key="SliderRepeatButtonTransparent" TargetType="RepeatButton">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Rectangle Fill="Transparent"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Slider Thumb 隐式样式 -->
    <Style TargetType="{x:Type Thumb}" x:Key="SliderThumbStyle">
        <Setter Property="Width" Value="12"/>
        <Setter Property="Height" Value="18"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Grid>
                        <!-- 外发光 -->
                        <Border x:Name="GlowBorder" CornerRadius="2" Margin="-2" Opacity="0">
                            <Border.Background>
                                <RadialGradientBrush>
                                    <GradientStop Color="#6060B0F0" Offset="0"/>
                                    <GradientStop Color="#0060B0F0" Offset="1"/>
                                </RadialGradientBrush>
                            </Border.Background>
                        </Border>
                        
                        <!-- 主体 -->
                        <Border x:Name="ThumbBorder" 
                                CornerRadius="2"
                                BorderThickness="1">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Color="#FFFFFFFF" Offset="0"/>
                                    <GradientStop Color="#FFF0F0F0" Offset="0.4"/>
                                    <GradientStop Color="#FFE0E0E0" Offset="0.5"/>
                                    <GradientStop Color="#FFF5F5F5" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.BorderBrush>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Color="#FF909090" Offset="0"/>
                                    <GradientStop Color="#FF707070" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.BorderBrush>
                            <Border.Effect>
                                <DropShadowEffect Color="#000000" BlurRadius="3" ShadowDepth="1" Opacity="0.4"/>
                            </Border.Effect>
                        </Border>
                        
                        <!-- 顶部高光 -->
                        <Border CornerRadius="2,2,0,0" Height="8" VerticalAlignment="Top" Margin="1,1,1,0" IsHitTestVisible="False">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Color="#80FFFFFF" Offset="0"/>
                                    <GradientStop Color="#00FFFFFF" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="GlowBorder" Property="Opacity" Value="0.8"/>
                            <Setter TargetName="ThumbBorder" Property="Background">
                                <Setter.Value>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#FFE8F4FF" Offset="0"/>
                                        <GradientStop Color="#FFD0E8FF" Offset="0.4"/>
                                        <GradientStop Color="#FFC0D8F0" Offset="0.5"/>
                                        <GradientStop Color="#FFD8ECFF" Offset="1"/>
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                            <Setter TargetName="ThumbBorder" Property="BorderBrush">
                                <Setter.Value>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#FF60A0D0" Offset="0"/>
                                        <GradientStop Color="#FF4080B0" Offset="1"/>
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter TargetName="GlowBorder" Property="Opacity" Value="1"/>
                            <Setter TargetName="ThumbBorder" Property="Background">
                                <Setter.Value>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#FFD0E8FF" Offset="0"/>
                                        <GradientStop Color="#FFB0D0F0" Offset="0.4"/>
                                        <GradientStop Color="#FFA0C0E0" Offset="0.5"/>
                                        <GradientStop Color="#FFC0D8F0" Offset="1"/>
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--                                                                                               -->
    <!--                                  RadioButton 隐式样式                                         -->
    <!--                                                                                               -->
    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--                                                                                               -->
    <!--  此样式自动应用于所有 RadioButton 控件，提供 Aero 风格的单选按钮外观。                           -->
    <!--  设计来源：Designs/RadioButtonBackground.xaml, RadioButtonInnerCircle.xaml                     -->
    <!--                                                                                               -->
    <!--  设计说明：                                                                                    -->
    <!--      ● 背景：圆形深蓝渐变，带高光边框                                                          -->
    <!--      ● 选中状态：内圆使用径向渐变，带 DropShadow 发光效果                                       -->
    <!--      ● 悬停状态：边框和背景增亮                                                                 -->
    <!--                                                                                               -->
    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->

    <Style TargetType="{x:Type RadioButton}">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="4,0,0,0"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RadioButton}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- RadioButton 圆形背景区域 -->
                        <Grid Width="16" Height="16" VerticalAlignment="Center">
                            <!-- 背景层 - 基于 RadioButtonBackground.xaml (Alpha 80%) -->
                            <Ellipse x:Name="Background" StrokeThickness="1">
                                <Ellipse.Stroke>
                                    <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                        <GradientStop Color="#66EAF2FE" Offset="0"/>
                                        <GradientStop Color="#00B7D7EE" Offset="0.528"/>
                                        <GradientStop Color="#668CC5E6" Offset="1"/>
                                    </LinearGradientBrush>
                                </Ellipse.Stroke>
                                <Ellipse.Fill>
                                    <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                        <GradientStop Color="#CCE4EAF8" Offset="0"/>
                                        <GradientStop Color="#CCA9B0BE" Offset="0.118"/>
                                        <GradientStop Color="#CC34526A" Offset="0.397"/>
                                        <GradientStop Color="#CC0D2D42" Offset="0.519"/>
                                        <GradientStop Color="#CC4C9EC0" Offset="1"/>
                                    </LinearGradientBrush>
                                </Ellipse.Fill>
                            </Ellipse>
                            
                            <!-- 悬停状态背景 (Alpha 80%) -->
                            <Ellipse x:Name="HoverBackground" Opacity="0" StrokeThickness="1">
                                <Ellipse.Stroke>
                                    <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                        <GradientStop Color="#99EAF2FE" Offset="0"/>
                                        <GradientStop Color="#33B7D7EE" Offset="0.528"/>
                                        <GradientStop Color="#998CC5E6" Offset="1"/>
                                    </LinearGradientBrush>
                                </Ellipse.Stroke>
                                <Ellipse.Fill>
                                    <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                        <GradientStop Color="#CCEEF4FF" Offset="0"/>
                                        <GradientStop Color="#CCB9C0CE" Offset="0.118"/>
                                        <GradientStop Color="#CC44627A" Offset="0.397"/>
                                        <GradientStop Color="#CC1D3D52" Offset="0.519"/>
                                        <GradientStop Color="#CC5CAED0" Offset="1"/>
                                    </LinearGradientBrush>
                                </Ellipse.Fill>
                            </Ellipse>
                            
                            <!-- 选中状态内圆 - 基于 RadioButtonInnerCircle.xaml + DropShadow -->
                            <Ellipse x:Name="CheckMark" 
                                     Width="8" Height="8" 
                                     Opacity="0">
                                <Ellipse.Fill>
                                    <RadialGradientBrush RadiusX="1.15" RadiusY="1.15" Center="0.5,1.15" GradientOrigin="0.5,1.15">
                                        <GradientStop Color="#FF8AE0FF" Offset="0.093"/>
                                        <GradientStop Color="#FF35A6E6" Offset="0.645"/>
                                        <GradientStop Color="#FF4DA6E4" Offset="0.712"/>
                                        <GradientStop Color="#FFAED3F4" Offset="0.942"/>
                                    </RadialGradientBrush>
                                </Ellipse.Fill>
                                <!-- DropShadow 效果：Softness=2, Offset=0, Opacity=0.8, Color=#22657C -->
                                <Ellipse.Effect>
                                    <DropShadowEffect Color="#22657C" BlurRadius="2" ShadowDepth="0" Opacity="0.8" Direction="315"/>
                                </Ellipse.Effect>
                            </Ellipse>
                        </Grid>
                        
                        <!-- 内容区域 -->
                        <ContentPresenter Grid.Column="1"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          RecognizesAccessKey="True">
                            <ContentPresenter.Effect>
                                <DropShadowEffect Color="#000000" BlurRadius="2" ShadowDepth="1" Opacity="0.5"/>
                            </ContentPresenter.Effect>
                        </ContentPresenter>
                    </Grid>
                    
                    <ControlTemplate.Triggers>
                        <!-- 悬停状态触发器 -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="HoverBackground" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="HoverBackground" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        
                        <!-- 选中状态触发器 -->
                        <Trigger Property="IsChecked" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="CheckMark" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="CheckMark" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        
                        <!-- 禁用状态触发器 -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--                                                                                               -->
    <!--                                  CheckBox 隐式样式                                            -->
    <!--                                                                                               -->
    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--                                                                                               -->
    <!--  此样式自动应用于所有 CheckBox 控件，提供 Aero 风格的复选框外观。                                -->
    <!--  设计来源：Designs/CheckboxBackground.xaml, RadioButtonInnerCircle.xaml (对勾样式参考)          -->
    <!--                                                                                               -->
    <!--  设计说明：                                                                                    -->
    <!--      ● 背景：圆角矩形深蓝渐变，带高光边框                                                       -->
    <!--      ● 选中状态：对勾使用与 RadioButton 内圆相同的径向渐变，带 DropShadow 发光效果               -->
    <!--      ● 悬停状态：边框和背景增亮                                                                 -->
    <!--                                                                                               -->
    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->

    <Style TargetType="{x:Type CheckBox}">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="4,0,0,0"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CheckBox}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- CheckBox 方形背景区域 -->
                        <Grid Width="16" Height="16" VerticalAlignment="Center">
                            <!-- 背景层 - 基于 CheckboxBackground.xaml -->
                            <Border x:Name="Background" CornerRadius="2" BorderThickness="1">
                                <Border.BorderBrush>
                                    <LinearGradientBrush StartPoint="0.5,-1.45" EndPoint="0.5,1.73">
                                        <GradientStop Color="#CCD9E7F4" Offset="0"/>
                                        <GradientStop Color="#CC7CBEEA" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.BorderBrush>
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0.5,-1.46" EndPoint="0.5,1.74">
                                        <GradientStop Color="#CC9CB3C8" Offset="0.473"/>
                                        <GradientStop Color="#CC3A576E" Offset="0.593"/>
                                        <GradientStop Color="#CC162D41" Offset="0.623"/>
                                        <GradientStop Color="#CC4C87AF" Offset="0.798"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                            </Border>
                            
                            <!-- 悬停状态背景 -->
                            <Border x:Name="HoverBackground" Opacity="0" CornerRadius="2" BorderThickness="1">
                                <Border.BorderBrush>
                                    <LinearGradientBrush StartPoint="0.5,-1.45" EndPoint="0.5,1.73">
                                        <GradientStop Color="#FFE9F7FF" Offset="0"/>
                                        <GradientStop Color="#FF8CCEFA" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.BorderBrush>
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0.5,-1.46" EndPoint="0.5,1.74">
                                        <GradientStop Color="#FFACC3D8" Offset="0.473"/>
                                        <GradientStop Color="#FF4A677E" Offset="0.593"/>
                                        <GradientStop Color="#FF263D51" Offset="0.623"/>
                                        <GradientStop Color="#FF5C97BF" Offset="0.798"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                            </Border>
                            
                            <!-- 选中状态对勾 - 参考 RadioButtonInnerCircle.xaml 的样式 + DropShadow -->
                            <Path x:Name="CheckMark" 
                                  Data="M 2,7 L 6,11 L 13,3"
                                  StrokeThickness="2"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"
                                  StrokeLineJoin="Round"
                                  Opacity="0"
                                  Margin="0,-1,0,0">
                                <Path.Stroke>
                                    <RadialGradientBrush RadiusX="1.15" RadiusY="1.15" Center="0.5,1.15" GradientOrigin="0.5,1.15">
                                        <GradientStop Color="#FF8AE0FF" Offset="0.093"/>
                                        <GradientStop Color="#FF35A6E6" Offset="0.645"/>
                                        <GradientStop Color="#FF4DA6E4" Offset="0.712"/>
                                        <GradientStop Color="#FFAED3F4" Offset="0.942"/>
                                    </RadialGradientBrush>
                                </Path.Stroke>
                                <!-- DropShadow 效果：Softness=2, Offset=0, Opacity=0.8, Color=#22657C -->
                                <Path.Effect>
                                    <DropShadowEffect Color="#22657C" BlurRadius="2" ShadowDepth="0" Opacity="0.8" Direction="315"/>
                                </Path.Effect>
                            </Path>
                            
                            <!-- 不确定状态标记 -->
                            <Border x:Name="IndeterminateMark"
                                    Width="8" Height="8"
                                    CornerRadius="1"
                                    Opacity="0">
                                <Border.Background>
                                    <RadialGradientBrush RadiusX="1.15" RadiusY="1.15" Center="0.5,1.15" GradientOrigin="0.5,1.15">
                                        <GradientStop Color="#FF8AE0FF" Offset="0.093"/>
                                        <GradientStop Color="#FF35A6E6" Offset="0.645"/>
                                        <GradientStop Color="#FF4DA6E4" Offset="0.712"/>
                                        <GradientStop Color="#FFAED3F4" Offset="0.942"/>
                                    </RadialGradientBrush>
                                </Border.Background>
                                <Border.Effect>
                                    <DropShadowEffect Color="#22657C" BlurRadius="2" ShadowDepth="0" Opacity="0.8" Direction="315"/>
                                </Border.Effect>
                            </Border>
                        </Grid>
                        
                        <!-- 内容区域 -->
                        <ContentPresenter Grid.Column="1"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          RecognizesAccessKey="True">
                            <ContentPresenter.Effect>
                                <DropShadowEffect Color="#000000" BlurRadius="2" ShadowDepth="1" Opacity="0.5"/>
                            </ContentPresenter.Effect>
                        </ContentPresenter>
                    </Grid>
                    
                    <ControlTemplate.Triggers>
                        <!-- 悬停状态触发器 -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="HoverBackground" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="HoverBackground" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        
                        <!-- 选中状态触发器 -->
                        <Trigger Property="IsChecked" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="CheckMark" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.05"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="CheckMark" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.05"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        
                        <!-- 不确定状态触发器 -->
                        <Trigger Property="IsChecked" Value="{x:Null}">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="IndeterminateMark" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.05"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="IndeterminateMark" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.05"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        
                        <!-- 禁用状态触发器 -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Slider 隐式样式 -->
    <Style TargetType="{x:Type Slider}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Slider">
                    <Grid VerticalAlignment="Center">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto" MinHeight="{TemplateBinding MinHeight}"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <!-- 轨道背景 -->
                        <Border x:Name="TrackBackground"
                                Grid.Row="1"
                                Height="6"
                                CornerRadius="3"
                                VerticalAlignment="Center">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Color="#60000000" Offset="0"/>
                                    <GradientStop Color="#40000000" Offset="0.5"/>
                                    <GradientStop Color="#30000000" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.BorderBrush>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Color="#40000000" Offset="0"/>
                                    <GradientStop Color="#20FFFFFF" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.BorderBrush>
                            <Border.BorderThickness>1</Border.BorderThickness>
                        </Border>
                        
                        <!-- 进度轨道 -->
                        <Track x:Name="PART_Track" Grid.Row="1">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="{x:Static Slider.DecreaseLarge}" Style="{StaticResource SliderRepeatButtonTransparent}">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton">
                                            <Border Height="4" CornerRadius="2" VerticalAlignment="Center" Margin="2,0,0,0">
                                                <Border.Background>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                        <GradientStop Color="#FF80D0FF" Offset="0"/>
                                                        <GradientStop Color="#FF40A0E0" Offset="0.4"/>
                                                        <GradientStop Color="#FF0080D0" Offset="0.5"/>
                                                        <GradientStop Color="#FF60B0E0" Offset="1"/>
                                                    </LinearGradientBrush>
                                                </Border.Background>
                                                <!-- 进度条发光效果 -->
                                                <Border CornerRadius="2" Margin="-1">
                                                    <Border.Effect>
                                                        <DropShadowEffect Color="#4080C0FF" BlurRadius="4" ShadowDepth="0" Opacity="0.6"/>
                                                    </Border.Effect>
                                                </Border>
                                            </Border>
                                        </ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.DecreaseRepeatButton>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="{x:Static Slider.IncreaseLarge}" Style="{StaticResource SliderRepeatButtonTransparent}"/>
                            </Track.IncreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Style="{StaticResource SliderThumbStyle}"/>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                    
                    <!-- 垂直方向触发器 -->
                    <ControlTemplate.Triggers>
                        <Trigger Property="Orientation" Value="Vertical">
                            <Setter TargetName="TrackBackground" Property="Width" Value="6"/>
                            <Setter TargetName="TrackBackground" Property="Height" Value="Auto"/>
                            <Setter TargetName="TrackBackground" Property="HorizontalAlignment" Value="Center"/>
                            <Setter TargetName="TrackBackground" Property="VerticalAlignment" Value="Stretch"/>
                            <Setter TargetName="PART_Track" Property="Orientation" Value="Vertical"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--                                                                                               -->
    <!--                                  ListBox 隐式样式                                             -->
    <!--                                                                                               -->
    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--                                                                                               -->
    <!--  此样式自动应用于所有 ListBox 控件，提供 Aero 风格的列表框外观。                                 -->
    <!--  设计来源：Designs/ListBoxItemBackground.png, ListBoxItemHover.xaml, ListBoxItemSelected.xaml -->
    <!--                                                                                               -->
    <!--  设计说明：                                                                                    -->
    <!--      ● 背景：使用 ListBoxItemBackground.png 作为背景图片                                        -->
    <!--      ● 内阴影：四周渐变内阴影，增强深度感                                                       -->
    <!--      ● 列表项悬停：青蓝色径向发光                                                              -->
    <!--      ● 列表项选中：更强的青蓝色径向发光                                                        -->
    <!--                                                                                               -->
    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->

    <!-- ListBoxItem 隐式样式 -->
    <Style TargetType="{x:Type ListBoxItem}">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="MinHeight" Value="32"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                    <Grid>
                        <!-- 悬停状态背景 - 基于 ListBoxItemHover.xaml -->
                        <Border x:Name="HoverBackground" Opacity="0" CornerRadius="3" BorderThickness="1">
                            <Border.BorderBrush>
                                <RadialGradientBrush RadiusX="1.181" RadiusY="1.095" Center="0.5,-0.195" GradientOrigin="0.5,-0.195">
                                    <GradientStop Color="#FF5984AD" Offset="0"/>
                                    <GradientStop Color="#FFFFFFFF" Offset="1"/>
                                </RadialGradientBrush>
                            </Border.BorderBrush>
                            <Border.Background>
                                <RadialGradientBrush RadiusX="1.182" RadiusY="1.104" Center="0.5,-0.201" GradientOrigin="0.5,-0.201">
                                    <GradientStop Color="#374588BD" Offset="0"/>
                                    <GradientStop Color="#081AD5FF" Offset="0.691"/>
                                    <GradientStop Color="#1FFFFFFF" Offset="1"/>
                                </RadialGradientBrush>
                            </Border.Background>
                        </Border>
                        
                        <!-- 选中状态背景 - 基于 ListBoxItemSelected.xaml -->
                        <Border x:Name="SelectedBackground" Opacity="0" CornerRadius="3" BorderThickness="1">
                            <Border.BorderBrush>
                                <RadialGradientBrush RadiusX="1.181" RadiusY="1.095" Center="0.5,-0.195" GradientOrigin="0.5,-0.195">
                                    <GradientStop Color="#FF5984AD" Offset="0"/>
                                    <GradientStop Color="#FFFFFFFF" Offset="1"/>
                                </RadialGradientBrush>
                            </Border.BorderBrush>
                            <Border.Background>
                                <RadialGradientBrush RadiusX="1.182" RadiusY="1.104" Center="0.5,-0.201" GradientOrigin="0.5,-0.201">
                                    <GradientStop Color="#A34588BD" Offset="0"/>
                                    <GradientStop Color="#111AD5FF" Offset="0.691"/>
                                    <GradientStop Color="#31FFFFFF" Offset="1"/>
                                </RadialGradientBrush>
                            </Border.Background>
                        </Border>
                        
                        <!-- 内容呈现器 -->
                        <ContentPresenter Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                            <ContentPresenter.Effect>
                                <DropShadowEffect Color="#000000" BlurRadius="2" ShadowDepth="1" Opacity="0.3"/>
                            </ContentPresenter.Effect>
                        </ContentPresenter>
                    </Grid>
                    
                    <ControlTemplate.Triggers>
                        <!-- 悬停状态触发器 -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="HoverBackground" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="HoverBackground" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        
                        <!-- 选中状态触发器 -->
                        <Trigger Property="IsSelected" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="SelectedBackground" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="SelectedBackground" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        
                        <!-- 选中且悬停状态 - 保持选中状态的显示 -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="SelectedBackground" Property="Opacity" Value="1"/>
                            <Setter TargetName="HoverBackground" Property="Opacity" Value="0"/>
                        </MultiTrigger>
                        
                        <!-- 禁用状态触发器 -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ListBox 隐式样式 -->
    <Style TargetType="{x:Type ListBox}">
        <Setter Property="Foreground" Value="Transparent"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="4"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListBox}">
                    <Grid>
                        <!-- 主背景层 - 使用 ListBoxItemBackground.png -->
                        <Border CornerRadius="4">
                            <Border.Background>
                                <ImageBrush ImageSource="/InstallationWizard;component/Resources/ListBoxItemBackground.png" Stretch="Fill"/>
                            </Border.Background>
                        </Border>
                        
                        <!-- ═══════════════════════════════════════════════════════════════════════ -->
                        <!-- 内阴影效果 - 使用传统渐变实现                                              -->
                        <!-- ═══════════════════════════════════════════════════════════════════════ -->
                        
                        <!-- 顶部内阴影 -->
                        <Border CornerRadius="4,4,0,0" Height="12" VerticalAlignment="Top" IsHitTestVisible="False">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Color="#60000000" Offset="0"/>
                                    <GradientStop Color="#30000000" Offset="0.5"/>
                                    <GradientStop Color="#00000000" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                        
                        <!-- 左侧内阴影 -->
                        <Border CornerRadius="4,0,0,4" Width="8" HorizontalAlignment="Left" IsHitTestVisible="False">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#40000000" Offset="0"/>
                                    <GradientStop Color="#20000000" Offset="0.5"/>
                                    <GradientStop Color="#00000000" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                        
                        <!-- 右侧内阴影 -->
                        <Border CornerRadius="0,4,4,0" Width="8" HorizontalAlignment="Right" IsHitTestVisible="False">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="1,0" EndPoint="0,0">
                                    <GradientStop Color="#40000000" Offset="0"/>
                                    <GradientStop Color="#20000000" Offset="0.5"/>
                                    <GradientStop Color="#00000000" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                        
                        <!-- 底部内阴影 -->
                        <Border CornerRadius="0,0,4,4" Height="8" VerticalAlignment="Bottom" IsHitTestVisible="False">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,1" EndPoint="0,0">
                                    <GradientStop Color="#30000000" Offset="0"/>
                                    <GradientStop Color="#10000000" Offset="0.5"/>
                                    <GradientStop Color="#00000000" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                        
                        <!-- 内容区域 -->
                        <ScrollViewer Margin="6" Focusable="False">
                            <ItemsPresenter/>
                        </ScrollViewer>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--                                                                                               -->
    <!--                                ProgressBar 隐式样式                                           -->
    <!--                                                                                               -->
    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--                                                                                               -->
    <!--  此样式自动应用于所有 ProgressBar 控件，提供 Aero 风格的进度条外观。                             -->
    <!--  设计来源：Designs/ProgressBarContainer.xaml, ProgressBarContent.xaml                          -->
    <!--                                                                                               -->
    <!--  设计说明：                                                                                    -->
    <!--      ● 容器：圆角边框，径向渐变边框（青蓝色到白色），半透明白色渐变填充                           -->
    <!--      ● 进度条：青蓝色渐变填充，从浅青色到深青色，带有中间的深色分界                              -->
    <!--      ● 圆角：6px 圆角，保持 Aero 风格的柔和外观                                                 -->
    <!--                                                                                               -->
    <!-- ═══════════════════════════════════════════════════════════════════════════════════════════ -->

    <Style TargetType="{x:Type ProgressBar}">
        <Setter Property="Height" Value="24"/>
        <Setter Property="MinHeight" Value="24"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="#FF3D7FA8"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ProgressBar}">
                    <Grid>
                        <!-- ═══════════════════════════════════════════════════════════════════════ -->
                        <!-- 容器背景 - 基于 ProgressBarContainer.xaml                                 -->
                        <!-- ═══════════════════════════════════════════════════════════════════════ -->
                        
                        <!-- 容器：径向渐变边框 + 半透明白色渐变填充 -->
                        <Border x:Name="PART_Track" CornerRadius="2" BorderThickness="1">
                            <Border.BorderBrush>
                                <RadialGradientBrush RadiusX="1.105" RadiusY="0.470" Center="0.5,-0.052" GradientOrigin="0.5,-0.052">
                                    <RadialGradientBrush.RelativeTransform>
                                        <RotateTransform CenterX="0.5" CenterY="-0.052" Angle="90"/>
                                    </RadialGradientBrush.RelativeTransform>
                                    <GradientStop Color="#FF5984AD" Offset="0"/>
                                    <GradientStop Color="#FFFFFFFF" Offset="1"/>
                                </RadialGradientBrush>
                            </Border.BorderBrush>
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0.5,-0.057" EndPoint="0.5,1.057">
                                    <GradientStop Color="#3FFFFFFF" Offset="0"/>
                                    <GradientStop Color="#20000000" Offset="0.527"/>
                                    <GradientStop Color="#41FFFFFF" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            
                            <!-- ═══════════════════════════════════════════════════════════════════════ -->
                            <!-- 进度条内容 - 基于 ProgressBarContent.xaml                                 -->
                            <!-- ═══════════════════════════════════════════════════════════════════════ -->
                            
                            <!-- 进度条：青蓝色渐变填充 -->
                            <Border x:Name="PART_Indicator" 
                                    CornerRadius="1" 
                                    HorizontalAlignment="Left"
                                    Margin="2">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0.5,-0.225" EndPoint="0.5,1.225">
                                        <GradientStop Color="#FF95C8E2" Offset="0"/>
                                        <GradientStop Color="#FD3D7FA8" Offset="0.483"/>
                                        <GradientStop Color="#FC286792" Offset="0.512"/>
                                        <GradientStop Color="#FC46A1C9" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                            </Border>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
