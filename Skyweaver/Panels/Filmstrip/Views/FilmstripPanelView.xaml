<UserControl x:Class="Skyweaver.Panels.Filmstrip.Views.FilmstripPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="160"
             d:DesignWidth="520">
    <Border Style="{DynamicResource WorkAreaStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="{DynamicResource ToolbarBackgroundBrush}"
                    BorderBrush="{DynamicResource TransparentBrush}"
                    BorderThickness="0"
                    Padding="8,5">
                <TextBlock Text="{Binding Title}"
                           FontWeight="Bold"
                           Foreground="{DynamicResource LightTextBrush}"
                           FontFamily="{DynamicResource AeroFontFamily}"
                           FontSize="{DynamicResource AeroFontSize}"/>
            </Border>

            <Grid Grid.Row="1" Margin="12">
                <Border CornerRadius="10"
                        BorderBrush="#446FD4D1"
                        BorderThickness="1"
                        Background="#12000000"/>
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="{DynamicResource Common.PanelReady}"
                               Foreground="#FF96FCFF"
                               FontSize="18"
                               FontWeight="SemiBold"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding Description}"
                               Margin="0,10,0,0"
                               Foreground="#CCFFFFFF"
                               TextWrapping="Wrap"
                               TextAlignment="Center"
                               MaxWidth="420"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</UserControl>
