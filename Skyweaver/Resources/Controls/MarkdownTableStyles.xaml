<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Color x:Key="TwilightBlue_Header_Idle_StartColor">#FF63AADA</Color>
    <Color x:Key="TwilightBlue_Header_Idle_EndColor">#FFA0FCFF</Color>
    <Color x:Key="TwilightBlue_Header_Hover_StartColor">#FF7BC4F5</Color>
    <Color x:Key="TwilightBlue_Header_Hover_EndColor">#FFB8FDFF</Color>
    <Color x:Key="TwilightBlue_Header_Pressed_StartColor">#FF4B8CB8</Color>
    <Color x:Key="TwilightBlue_Header_Pressed_EndColor">#FF8ACCDD</Color>
    <Color x:Key="TwilightBlue_Selection_StartColor">#FF909FF7</Color>
    <Color x:Key="TwilightBlue_Selection_EndColor">#FF1D2550</Color>
    <Color x:Key="TwilightBlue_BorderColor">#FFDFE3E6</Color>

    <LinearGradientBrush x:Key="TwilightBlue_Header_Idle_Brush" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="{StaticResource TwilightBlue_Header_Idle_StartColor}" Offset="0.75"/>
        <GradientStop Color="{StaticResource TwilightBlue_Header_Idle_EndColor}" Offset="1.0"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="TwilightBlue_Header_Hover_Brush" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="{StaticResource TwilightBlue_Header_Hover_StartColor}" Offset="0.75"/>
        <GradientStop Color="{StaticResource TwilightBlue_Header_Hover_EndColor}" Offset="1.0"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="TwilightBlue_Header_Pressed_Brush" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="{StaticResource TwilightBlue_Header_Pressed_StartColor}" Offset="0.75"/>
        <GradientStop Color="{StaticResource TwilightBlue_Header_Pressed_EndColor}" Offset="1.0"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="TwilightBlue_Selection_Brush" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="{StaticResource TwilightBlue_Selection_StartColor}" Offset="0.008"/>
        <GradientStop Color="{StaticResource TwilightBlue_Selection_EndColor}" Offset="1.0"/>
    </LinearGradientBrush>

    <SolidColorBrush x:Key="TwilightBlue_BorderBrush" Color="{StaticResource TwilightBlue_BorderColor}"/>
    <SolidColorBrush x:Key="TwilightBlue_CellForegroundBrush" Color="#FF1B2A3B"/>

    <Style x:Key="TwilightBlue_DataGridColumnHeaderStyle" TargetType="{x:Type DataGridColumnHeader}">
        <Setter Property="Background" Value="{StaticResource TwilightBlue_Header_Idle_Brush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="8,5"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
        <Setter Property="BorderBrush" Value="{StaticResource TwilightBlue_BorderBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DataGridColumnHeader}">
                    <Grid x:Name="Root" SnapsToDevicePixels="True">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal"/>
                                <VisualState x:Name="MouseOver">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BackgroundRectangle" Storyboard.TargetProperty="Fill">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource TwilightBlue_Header_Hover_Brush}"/>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BackgroundRectangle" Storyboard.TargetProperty="Fill">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource TwilightBlue_Header_Pressed_Brush}"/>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>

                        <Rectangle x:Name="BackgroundRectangle"
                                   Fill="{TemplateBinding Background}"
                                   Stroke="{TemplateBinding BorderBrush}"
                                   StrokeThickness="1"
                                   Stretch="Fill"/>
                        <Grid Margin="{TemplateBinding Padding}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <ContentPresenter Grid.Column="0"
                                              Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>

                            <TextBlock x:Name="SortGlyph"
                                       Grid.Column="1"
                                       Margin="6,0,0,0"
                                       FontSize="10"
                                       FontWeight="Bold"
                                       Foreground="White"
                                       Visibility="Collapsed"/>
                        </Grid>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="SortDirection" Value="Ascending">
                            <Setter TargetName="SortGlyph" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="SortGlyph" Property="Text" Value="^"/>
                        </Trigger>
                        <Trigger Property="SortDirection" Value="Descending">
                            <Setter TargetName="SortGlyph" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="SortGlyph" Property="Text" Value="v"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TwilightBlue_DataGridRowStyle" TargetType="{x:Type DataGridRow}">
        <Setter Property="Foreground" Value="{StaticResource TwilightBlue_CellForegroundBrush}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource TwilightBlue_Selection_Brush}"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="TwilightBlue_DataGridCellStyle" TargetType="{x:Type DataGridCell}">
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="BorderBrush" Value="{StaticResource TwilightBlue_BorderBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="TextBlock.TextWrapping" Value="Wrap"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource TwilightBlue_Selection_Brush}"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="TwilightBlue_DataGridStyle" TargetType="{x:Type DataGrid}">
        <Setter Property="AutoGenerateColumns" Value="False"/>
        <Setter Property="CanUserAddRows" Value="False"/>
        <Setter Property="CanUserDeleteRows" Value="False"/>
        <Setter Property="CanUserReorderColumns" Value="False"/>
        <Setter Property="CanUserResizeColumns" Value="False"/>
        <Setter Property="CanUserResizeRows" Value="False"/>
        <Setter Property="CanUserSortColumns" Value="True"/>
        <Setter Property="ColumnHeaderStyle" Value="{StaticResource TwilightBlue_DataGridColumnHeaderStyle}"/>
        <Setter Property="RowStyle" Value="{StaticResource TwilightBlue_DataGridRowStyle}"/>
        <Setter Property="CellStyle" Value="{StaticResource TwilightBlue_DataGridCellStyle}"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="Foreground" Value="{StaticResource TwilightBlue_CellForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource TwilightBlue_BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="RowBackground" Value="White"/>
        <Setter Property="AlternatingRowBackground" Value="#FFF2F5F7"/>
        <Setter Property="GridLinesVisibility" Value="All"/>
        <Setter Property="VerticalGridLinesBrush" Value="{StaticResource TwilightBlue_BorderBrush}"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource TwilightBlue_BorderBrush}"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="IsReadOnly" Value="True"/>
        <Setter Property="SelectionMode" Value="Single"/>
        <Setter Property="SelectionUnit" Value="FullRow"/>
        <Setter Property="RowHeaderWidth" Value="0"/>
        <Setter Property="MinColumnWidth" Value="72"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
    </Style>
</ResourceDictionary>
